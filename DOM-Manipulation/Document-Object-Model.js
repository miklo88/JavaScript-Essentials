const DOM = "Document Object Model";
const subDOM = 10;

if (subDOM != 10) {
  console.log("Hey now");
} else if (DOM === "Document Object Model" && subDOM === 10) {
  console.log(DOM);
} else {
  console.log("uh oh, we have a bigger problem on our hands");
}
